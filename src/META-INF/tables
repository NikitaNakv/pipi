create table users (
name varchar(100),
password int NOT NULL,
primary key (name)
);

create table points (
id integer DEFAULT nextval('JPA_SEQUENCE'),
x real NOT NULL,
y real NOT NULL,
r real NOT NULL,
result boolean,
name varchar(100) REFERENCES users(name),
primary key (id)
);